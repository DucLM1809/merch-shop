node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

*storybook.log
storybook-static

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

graphify-out/cost.json        # local only
# graphify-out/cache/         # optional: commit for speed, skip to keep repo small

# bv (beads viewer) local config and caches
.bv/

coverage/

# Ignore Lighthouse CI directories and reports
.lighthouseci/
.lighthouseci-*
